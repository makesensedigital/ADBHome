<!-- Bucket 4 of the `adopt-an-existing-repository` triage. -->

# Open definitions

Decisions this site needs and nobody has taken.

**An entry here means permitted, not blocked.** Per the Handbook Guidelines, what is not governed is
permitted and reported: you decide, you proceed, and the decision lands here so the next person is
not deciding it again from nothing. If an open row stopped work, this file would become a list of
stop signs — and nobody writes those down, which loses the visibility that is the whole point.

Every row carries **the default that holds while it is open**, because a row without one is not a
decision deferred, it is a decision taken by accident.

Distinct from [`technical-debt.md`](technical-debt.md): that records what is *wrong*, which is not
permitted. This records what has not been *decided*, which is.

---

## Open

| # | Open question | Governing rule | Default while open | Opened |
|---|---|---|---|---|
| **4** | **Who answers an enquiry, by name.** | §26 — every conversion path has a named owner | **Nobody named.** `config.receiver.owner` is unanswered. The site presents no form, which is correct when nothing persists a submission — but the other half of the same rule wants a person, and messaging with no named owner is how an enquiry goes unanswered with nothing recording that it did. | 2026-08-11 |
| **5** | **Whether the publication origin moves from the branch to the pipeline.** `gate.yml` carries the `publish` job §26 requires, and it will not run while the repository's Pages source is *deploy from a branch*. | §26 — the delivery pipeline is the publication origin, mandatory | **The branch publishes, and nothing can refuse.** The push *is* the deploy, which is the structural reason this class of work tends to have no verification at all. **Flipping it is a repository setting, and it is the owner's to flip** — Settings → Pages → Source → GitHub Actions. Two things to know before doing so: the gate is currently red, so publication would stop until the buckets above are worked through; and `CNAME` must travel with the artifact or the custom domain unbinds **silently**, which the publish job asserts. | 2026-08-11 |
| **7** | **What the two quoting controls should point at.** *Sitio de cotización* in the footer's social row and *Cotizá tu seguro online* in its contact list both resolve to this page's own canonical URL — a visitor who clicks either lands where they already were. Commit `b5c51e1` pointed them there deliberately, which makes this a known state rather than a slip. | §26 — no conversion control resolves to the page it sits on | **They stay, and they are known-broken.** §26 gives two remedies and neither is "point it at the home page": build the destination, or remove the control. Both are the owner's call — one is a project, the other removes something visible from a client's site. Note that the gate does **not** catch these: the check keys on a button class or an analytics attribute and these anchors carry neither, so this row is the only thing recording them. | 2026-08-11 |
| **8** | **Whether this site gets an OpenSpec workspace.** §26 does not suspend §19, and `templates/landing` ships none. | §19 · §26 | **No workspace.** For a site whose whole change history is *add a section, fix the copy*, the brief and the gate may genuinely be enough — but that is a claim nobody has tested, and it is carried upstream as the handbook's own debt #13. Answer it from this site's real history, not from principle. If it turns out to be needed, the months before it have no archive and the reasoning cannot be reconstructed. | 2026-08-11 |
| **9** | **Whether a one-person repository adopts the pull-request flow now.** Seven commits, all direct to `main`, no merge, no `CODEOWNERS`. §26 says the default branch is not committed to directly and work arrives by pull request. | §21 · §26 | **Direct commits continue.** A code-owner file with one name requests review from the author, which no host grants, so it would be a mechanism that cannot fire — and required review needs branch protection, which is a plan question. **This becomes real the moment a second person can merge**, and that is the trigger, not a date. Adopting the flow alone is still worth something before then: it is what makes the gate run *before* publication rather than after. | 2026-08-11 |
| **10** | **Who owns this site after launch, and how it transfers.** No named owner, no review cadence, no scheduled check, and no ownership-and-exit inventory. | §26 — after launch, mandatory | **Nobody, and nothing is watched.** Nothing in this architecture announces a failure: no logs, no alerts, no health endpoint. A broken conversion control lives until somebody notices by chance. The inventory §26 asks for — registrar, DNS zone, domain verification, repository, hosting account, analytics property, tag container, form receiver, mailing platform, scheduling links — is the one that matters most on a client site: the unclaimed pieces are the ones that end up as **a DNS record pointing at a resource that no longer exists**, which is how a client's domain becomes claimable by a stranger. | 2026-08-11 |
| **12** | **Whether the Central Standards caller is ever able to run.** `.github/workflows/standards.yml` is pinned to `makesensedigital/engineering-handbook@v3` as §0 mandates. On its first execution — run `31555147024`, 2026-08-12 — it failed in **0 seconds**, before checking out anything: the handbook repository is private, and calling a reusable workflow across repositories requires an Actions access setting on **that** repository, not on this one. | §0 · §19 — the CI caller | **The caller stays, and it stays red.** Removing it would be worse than a failing job: it is the only thing recording that this repository is bound to a released handbook version, and §0 requires it. Keeping it means one permanently failing check that is **not about this site** — which is the shape of red that gets gates ignored, so it is worth closing rather than tolerating. **Two ways to close it:** grant Actions access from the handbook repository (Settings → Actions → Access → *Accessible from repositories in the organization*), or accept that it cannot run here and say so in the workflow file itself, by name. What it would enforce for a static site is thin in any case — the version manifest, and greps for backend patterns that do not exist here — so the cost of the second answer is small and should be measured against the cost of a check nobody can make green. | 2026-08-12 |

| **13** | **Who checks the tag container against the privacy statement, and when.** The container is configured by another team; `privacy.html` is published from this repository and describes what the container collects. Nothing connects the two. A tag added over there makes a legal document over here false, silently, and neither side has a step that would notice. | §26 — consent is an explicit recorded decision · the privacy statement is mandatory | **Nobody, and nothing is watched.** The default is the dangerous one: both teams are behaving correctly in isolation. `config.analytics.publishedCommitments` is the artifact to check against — it exists so the question has an answerable form — but an artifact with no reader is not a mechanism. **Cheapest thing that would work:** name one person who owns `privacy.html`, and make a container version bump require their sign-off. **Revisit immediately if** Google Signals, any advertising or remarketing tag, or any audience sharing is switched on — those are not gradual, they flip the document from true to false in one click. | 2026-08-13 |
---

## How to close a row

1. Decide. Write what was decided, who decided it and the date **into the row**, then move it below.
2. If the decision changes a value, change it in `config.js` in the same commit — a decision recorded
   here and not reflected there is a decision that did not happen.
3. If it removes a finding, run `node scripts/ratchet.mjs --update` in that same commit too. A
   baseline left high keeps headroom, and the violation just removed can come back for free.

## Closed

| # | Was | Closed by | When |
|---|---|---|---|
| 1 | Whether this site is instrumented at all, and with what | **Decided 2026-08-13 by Juan Torresel: instrumented, through a Google Tag Manager container (`GTM-M67M4S9B`) firing Google Analytics 4 and Microsoft Clarity.** No explicit event of this site's own — GA4 enhanced measurement covers page views, scroll and outbound clicks, which is the whole of what a brochure site does. **This decision replaced an earlier one taken the same day** (Amplitude, product analytics, session replay sampling nothing), which was implemented and then reverted unshipped; nothing of it reached production. **What did NOT change and cannot:** the three and a half weeks published between 2026-07-20 and today have no data and never will. This row closes because the gap stopped growing, not because it was filled. **Two consequences to carry.** The site observes a WhatsApp departure and nothing after it, so no conversion here is measurable end to end (`conversionsAreObservable: false`). And the container is a **publish surface the gate cannot see** — a tag added in the Tag Manager UI reaches every visitor with no commit and no review, which is debt D10 and the reason this row closes with a cost attached rather than cleanly. **Revisit when** the WhatsApp handoff is replaced by anything completing on this domain — that is the day a real conversion becomes measurable and an explicit event taxonomy starts to earn its keep. | 2026-08-13 |
| 2 | The consent decision — who takes it and when | **Decided 2026-08-13 by Juan Torresel: notice-only, no banner, WITH session recording and first-party cookies.** GA4 sets `_ga`; Clarity sets `_clck`/`_clsk` and replays the screen. The privacy statement was rewritten in the same change — §2 now declares the recording, the heatmaps and the cookies, and §5 names Google and Microsoft as the two parties this site *sends* to, plus the transfer to the United States. **That ordering was the load-bearing part: the document had to be true before the container was allowed to load.** **This reversed a decision taken hours earlier the same day**, which had refused session recording on the grounds that the policy could not carry it. The reversal is recorded rather than tidied away, because the reasoning that produced the first answer is what makes the second one legible: the fix was never to keep the tag out, it was to stop the document from claiming something untrue. **One fact that materially helps here:** the site presents no form, so a recording cannot capture anything a visitor typed — there is nothing to type. What it captures is navigation. **What is recorded rather than validated:** whether notice-without-consent is adequate for cookies and session recording under Ley 25.326 is the decider's call, not the gate's, and the case for confirming it with counsel is stronger than it was for analytics alone. **Revisit when** any advertising, remarketing or audience-sharing tag enters the container (Google Signals counts), Clarity masking is loosened below its default, or a form starts collecting what messaging collects today. The architectural limit is unchanged: this site cannot produce auditable proof of consent, which costs nothing while the mode is notice and costs a receiver the day a choice is offered. | 2026-08-13 |
| 6 | Whether this site stays on a host that can express none of §26's edge controls | **Decided 2026-08-12 by Juan Torresel: it stays on GitHub Pages, and the controls it puts out of reach are DECLARED ABSENT rather than left implied.** §26 permits exactly this — *choose against capability, record the choice, and record what the choice puts out of reach* — so this is a decision taken inside the rule, not an exception to it. The record is `config.controls` in `config.js`, which answers all ten rows of §26's control table. What is out of reach, named rather than softened: **no framing protection at all** (frame-ancestors is ignored in a meta element and X-Frame-Options is header-only, so this one has no in-document equivalent), no enforceable content security policy, no permissions policy, and no real redirect — which makes the canonical-identity rule unimplementable rather than merely unimplemented. `_headers` and `_redirects` do not exist here because on this host they would be files implying a control that is not in force. **Revisit when** a URL has to be retired: that is the day the missing redirect stops being theoretical, and it cannot be fixed after the fact. A change of host is the only remedy, and it is cheaper before the URLs exist than after. | 2026-08-12 |
| 3 | Which mailbox is canonical, and on which domain | **Decided 2026-08-12 by Juan Torresel: `info@adbseguros.com.ar`.** It is on the site's own domain, so the canonical mailbox and the canonical domain now agree, and `config.js` carries it. It unblocked fix-now F1 the same day — a privacy statement has to name where a rights request is sent. **What did not close:** the three offices publishing an address in the presence map are on `adbseguros.com`, a different domain. That is now a known discrepancy rather than an open question, and it is worth confirming the second domain is still ours and still names a resource that exists — a DNS record outliving what it points at is the failure §26 singles out on client work. | 2026-08-12 |
| 11 | Whether this repository's engineering record is served on the client's domain | **Decided 2026-08-11 by Juan Torresel: it stays served.** Nothing in it is a secret or internal material in §4c's sense, and `templates/landing` takes the same posture. The consequence is accepted with open eyes rather than discovered later: `adoption.md` and `technical-debt.md` are a public inventory of this site's compliance gaps on the broker's own domain, `robots.txt` invites every AI crawler, and a retrieval engine may quote them when asked about ADB. **Revisit if** a finding here is ever quoted back at the business, or if the repository stops being the publication origin — moving them then means moving them out of the web root, never into `.gitignore`, since an untracked record is gone by the next clone. | 2026-08-11 |
